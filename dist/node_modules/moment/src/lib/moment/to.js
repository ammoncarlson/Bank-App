"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const create_1 = require("../duration/create");
const local_1 = require("../create/local");
const constructor_1 = require("../moment/constructor");
function to(time, withoutSuffix) {
    if (this.isValid() &&
        ((constructor_1.isMoment(time) && time.isValid()) ||
            local_1.createLocal(time).isValid())) {
        return create_1.createDuration({ from: this, to: time }).locale(this.locale()).humanize(!withoutSuffix);
    }
    else {
        return this.localeData().invalidDate();
    }
}
exports.to = to;
function toNow(withoutSuffix) {
    return this.to(local_1.createLocal(), withoutSuffix);
}
exports.toNow = toNow;
//# sourceMappingURL=to.js.map