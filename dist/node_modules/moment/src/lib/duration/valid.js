"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const to_int_1 = require("../utils/to-int");
const index_of_1 = require("../utils/index-of");
const create_1 = require("./create");
var ordering = ['year', 'quarter', 'month', 'week', 'day', 'hour', 'minute', 'second', 'millisecond'];
function isDurationValid(m) {
    for (var key in m) {
        if (!(index_of_1.default.call(ordering, key) !== -1 && (m[key] == null || !isNaN(m[key])))) {
            return false;
        }
    }
    var unitHasDecimal = false;
    for (var i = 0; i < ordering.length; ++i) {
        if (m[ordering[i]]) {
            if (unitHasDecimal) {
                return false; // only allow non-integers for smallest unit
            }
            if (parseFloat(m[ordering[i]]) !== to_int_1.default(m[ordering[i]])) {
                unitHasDecimal = true;
            }
        }
    }
    return true;
}
exports.default = isDurationValid;
function isValid() {
    return this._isValid;
}
exports.isValid = isValid;
function createInvalid() {
    return create_1.createDuration(NaN);
}
exports.createInvalid = createInvalid;
//# sourceMappingURL=valid.js.map