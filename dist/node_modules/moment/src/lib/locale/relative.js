"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.defaultRelativeTime = {
    future: 'in %s',
    past: '%s ago',
    s: 'a few seconds',
    ss: '%d seconds',
    m: 'a minute',
    mm: '%d minutes',
    h: 'an hour',
    hh: '%d hours',
    d: 'a day',
    dd: '%d days',
    M: 'a month',
    MM: '%d months',
    y: 'a year',
    yy: '%d years'
};
const is_function_1 = require("../utils/is-function");
function relativeTime(number, withoutSuffix, string, isFuture) {
    var output = this._relativeTime[string];
    return (is_function_1.default(output)) ?
        output(number, withoutSuffix, string, isFuture) :
        output.replace(/%d/i, number);
}
exports.relativeTime = relativeTime;
function pastFuture(diff, output) {
    var format = this._relativeTime[diff > 0 ? 'future' : 'past'];
    return is_function_1.default(format) ? format(output) : format.replace(/%s/i, output);
}
exports.pastFuture = pastFuture;
//# sourceMappingURL=relative.js.map